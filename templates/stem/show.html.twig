{% extends 'base.html.twig' %}

{% block title %}Stem{% endblock %}

{% block body %}
    <h1>Stem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stem.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('stem_index') }}">back to list</a>

    <a href="{{ path('stem_edit', {'id': stem.id}) }}">edit</a>

    {{ include('stem/_delete_form.html.twig') }}
{% endblock %}
