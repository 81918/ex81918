{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <table>
        <tr>
            <th>username</th>
            <th>Acties</th>
        </tr>
    {% for user in users %}
        <tr>
            <td>{{ user.username }}</td>
            <td>{{ include('user/_delete_form.html.twig') }}</td>
        </tr>
    {% endfor %}
    </table>
{% endblock %}